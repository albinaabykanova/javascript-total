//  ? -----------MATIKSAL OPERATORLER---------------- logic operators

/*
    && (AND) : VE --- TÜM KOŞULLARIN DOĞRU OLMASI GEREKİR
    || (OR) : VEYA --- KOŞULLARDAN BİRİNİN DOĞRU OLMASI YETERLİ
    ! (NOT) : DEGIL --- KOŞULUN DEĞİLİ DEMEK 
*/

/*
let yas = 20;
let para = 3500;
let saglikliMi = false;
let hakSayisi = 3;

console.log((para > 3000 && hakSayisi > 1) || (yas > 18 && saglikliMi)); // true
*/
