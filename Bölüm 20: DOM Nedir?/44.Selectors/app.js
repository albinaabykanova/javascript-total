// ? SELECTORS (SEÇİCİLER) - STYLE ÖZELLİKLER

//classname , id, tag name

//querySelector - querySelectorsAll

/*
todoList.forEach(function (todo) {
  todo.style.backgroundColor = "pink";
});

console.log(todoList);
*/
