// ? KOŞULLLAR --- CONDITIONALS

/*
let not = 65;
if (not > 50) {
  console.log("geçtiniz, notunuz : " + not);
} else {
  console.log("kaldınız, notunuz : " + not);
}
*/

/*
let yas = Number(prompt("Yaşınız : "));
console.log(typeof yas);
let para = Number(prompt("Paranız: "));

if (yas >= 18 && para >= 3000) {
  alert("Ehliyet Sınava katılabilirsiniz");
} else {
  alert("Ehliyet Sınava katılamazsınız");
}
*/

// --------------- DERS ORTALAMA BULMA ----------------

// vize1 %30, vize2 %30, final %40

/*
let vize1 = Number(prompt("Vize 1 notunuz giriniz : "));
let vize2 = Number(prompt("Vize 2 notunuz giriniz : "));

let final = Number(prompt("Final notunuz giriniz : "));

let ortalama = vize1 * 0.3 + vize2 * 0.3 + final * 0.4;

if (ortalama >= 60) {
  alert("Tebrikler, dersi geçtiniz. Ortalamanız :) " + ortalama);
  console.log("Tebrikler, dersi geçtiniz. Ortalamanız :) " + ortalama);
} else {
  alert("Dersten kaldınız. geçmiş olsun :() " + ortalama);
  console.log("Dersten kaldınız. geçmiş olsun :() " + ortalama);
}
*/
